# NVIDIA-vGPU-Transcoding-Automated-Installer
install_nvidia.sh - An automated bash script that bypasses Debian/Proxmox package conflicts (firmware-nvidia-gsp) to flawlessly compile and install the official NVIDIA 550.x drivers directly onto the Proxmox host.
